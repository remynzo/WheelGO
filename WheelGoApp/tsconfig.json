{
  // Esta linha é a mais importante.
  // Ela herda todas as configurações padrão e recomendadas para um projeto React Native.
  "extends": "@react-native/typescript-config/tsconfig.json",

  "compilerOptions": {
    // Aqui você pode adicionar ou sobrescrever regras específicas se precisar no futuro.
    // Por enquanto, deixar vazio está perfeito, pois já estamos herdando tudo da linha de cima.
  }
}
